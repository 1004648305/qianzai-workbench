# 倩崽工作台 · 产品需求文档（PRD v2.0）

**文档性质**：开发用需求规格说明书  
**版本**：2.0  
**最后更新**：2026-07-31  

---

## 一、产品概述

### 1.1 产品定位
一款INS博主风格的个人本地工作台，聚合日常管理功能。纯前端应用，所有数据存储于浏览器本地，无需网络和服务器，开箱即用。

### 1.2 核心特征
- 左侧纵向导航栏，含9个功能菜单 + 1个总览首页
- 所有数据本地留存，永不丢失
- 每日自动重置“今日计划”与“工作待办”（仅这两项）
- 顶部显示实时时钟，每天更新
- 整体风格：柔和低饱和、细圆角、弱阴影、舒适留白、耐看干净

---

## 二、全局规则（所有模块通用）

### 2.1 数据存储
- 全部数据存储于浏览器 localStorage。
- 用户关闭或刷新页面，数据不丢失。
- 页面底部提供“导出数据”和“导入数据”功能，方便用户手动备份和迁移。

### 2.2 跨日检测与自动重置
- 页面每次加载时，自动检测当前日期与上次访问日期是否一致。
- 若发现是新的一天，则自动触发以下操作：
  1. 对“今日计划”模块执行重置（规则详见模块6）。
  2. 对“工作待办”模块执行重置（规则详见模块7）。
  3. 更新系统记录的“最后访问日期”为今日。
  4. 页面顶部显示一条轻提示：“✨ 新的一天已重置计划与待办”，3秒后自动消失。
- 除上述两个模块外，其余7个模块（阅读、锻炼、吃饭、体重、理财、生活清单、旅游）**只累积记录，永不删除或重置**。

### 2.3 顶部时间显示
- 固定在页面顶部右侧。
- 显示格式：`2026年7月31日 星期五 14:32:08`（含秒数，实时跳动）。
- 使用系统时间，每秒刷新一次。

---

## 三、导航结构

左侧纵向导航栏，共10个入口（含总览）：

| 序号 | 菜单名称 | 图标（Emoji） | 说明 |
| :--- | :--- | :--- | :--- |
| 0 | 今日总览 | 📊 | 默认首页，展示今日摘要 |
| 1 | 每月阅读 | 📖 | 读书笔记记录 |
| 2 | 锻炼身体 | 💪 | 运动记录 |
| 3 | 好好吃饭 | 🍱 | 饮食记录 |
| 4 | 体重管理 | ⚖️ | 体重体脂记录 |
| 5 | 理财管理 | 💰 | 收支记录与统计 |
| 6 | 今日计划 | 📝 | 每日计划（参与重置） |
| 7 | 工作待办 | 💼 | 工作任务（参与重置） |
| 8 | 生活清单 | 🎀 | 各类心愿/购物/影单 |
| 9 | 旅游记录 | 🗺️ | 旅行记忆 |

**导航交互行为**：
- 默认宽度72px，仅显示图标。
- 鼠标悬浮时平滑展开至190px，显示图标+文字。
- 首次加载时，导航自动展开2秒再收回，让用户知道文字标签的存在。
- 点击任一菜单，右侧内容区域切换至对应面板，当前菜单项高亮。

---

## 四、各模块详细需求

### 模块0：今日总览（默认首页）

**性质**：纯展示页面，无新增/编辑功能。

**展示内容**：
1. 顶部问候语：根据当前时段显示“早上好 🌤️ / 下午好 ☀️ / 晚上好 🌙，倩崽”。
2. 今日日期显示。
3. **今日计划摘要**：显示“今日计划 · 已完成 X / 共 X 项”。
4. **工作待办摘要**：显示“待办事项 X 项（其中逾期 X 项）”。
5. **今日新增动态**：
   - 今日阅读记录数（本）
   - 今日锻炼记录数（次）
   - 今日吃饭记录数（餐）
   - 最新体重值（若有记录）
6. **快捷入口**：提供“📖 去记录阅读”、“💪 去记录锻炼”、“📝 去规划今日”三个文字链接，点击跳转至对应模块。

---

### 模块1：每月阅读

**功能**：记录每本书的阅读笔记。

**字段定义**：
| 字段 | 说明 | 是否必填 |
| :--- | :--- | :--- |
| 阅读日期 | 默认今日，可修改 | 是 |
| 书名 | 文本输入 | 是 |
| 作者 | 文本输入 | 否 |
| 摘抄 | 多行文本 | 否 |
| 所悟 | 多行文本 | 否 |
| 当前进度 | 如“P1-50 / 共300页” | 否 |
| 阅读状态 | 下拉选择：在读 / 读完 / 弃读 | 是 |
| 评分 | 1~5星（点击星星选择） | 否 |

**列表展示**：按日期倒序排列，每条记录显示书名、作者、状态标签、评分星星。

---

### 模块2：锻炼身体

**功能**：记录每日运动情况。

**字段定义**：
| 字段 | 说明 | 是否必填 |
| :--- | :--- | :--- |
| 锻炼日期 | 默认今日，可修改 | 是 |
| 运动类型 | 下拉选择：跑步 / 瑜伽 / 力量 / 有氧 / 骑行 / 其他 | 是 |
| 时长 | 数字输入，单位：分钟 | 是（须大于0） |
| 消耗卡路里 | 数字输入，估算值 | 否 |
| 运动感受 | 短文本，如“今天状态不错” | 否 |

**列表展示**：按日期倒序排列，每条记录显示运动类型、时长、感受。

**额外统计**：页面顶部显示“本月锻炼 X 天，累计 X 分钟”。

---

### 模块3：好好吃饭

**功能**：记录每日饮食情况。

**字段定义**：
| 字段 | 说明 | 是否必填 |
| :--- | :--- | :--- |
| 用餐日期 | 默认今日，可修改 | 是 |
| 餐别 | 下拉选择：早餐 / 午餐 / 晚餐 / 加餐 | 是 |
| 食物名称 | 文本输入 | 是 |
| 是否健康 | 单选：是 / 否 / 一般 | 是 |
| 备注 | 文本输入 | 否 |

**列表展示**：按日期倒序排列，每条记录显示餐别、食物名称、健康标签。

---

### 模块4：体重管理

**功能**：记录体重及体脂数据，追踪变化趋势。

**字段定义**：
| 字段 | 说明 | 是否必填 |
| :--- | :--- | :--- |
| 记录日期 | 默认今日，可修改 | 是 |
| 体重 | 数字输入，保留1位小数，单位kg | 是（须大于0） |
| 体脂率 | 数字输入，单位% | 否 |
| 备注 | 如“晨起空腹”、“姨妈期”等 | 否 |

**列表展示**：按日期倒序排列，每条记录显示体重、体脂率、备注。

**顶部统计**：显示“最新体重：XX.X kg”。

---

### 模块5：理财管理

**功能**：记录日常收支，自动统计月度汇总。

**字段定义**：
| 字段 | 说明 | 是否必填 |
| :--- | :--- | :--- |
| 日期 | 默认今日，可修改 | 是 |
| 类型 | 单选：收入 / 支出 | 是 |
| 类别 | 下拉选择：餐饮 / 交通 / 购物 / 娱乐 / 工资 / 其他 | 是 |
| 金额 | 数字输入，单位：元 | 是（须大于0） |
| 支付方式 | 下拉选择：微信 / 支付宝 / 现金 / 信用卡 | 是 |
| 备注 | 文本输入 | 否 |

**列表展示**：按日期倒序排列，收入金额显示为绿色，支出金额显示为橙色。

**顶部统计**：显示“本月收入：¥XXX · 本月支出：¥XXX · 结余：¥XXX”。

---

### 模块6：今日计划 ⚠️ 参与每日重置

**功能**：管理当天的待办事项，支持状态追踪。

**字段定义**：
| 字段 | 说明 | 是否必填 |
| :--- | :--- | :--- |
| 计划时间 | 如“09:00-10:00” | 否 |
| 事项内容 | 文本输入 | 是 |
| 优先级 | 下拉选择：高 / 中 / 低 | 是 |
| 完成状态 | 点击切换：未开始 / 进行中 / 已完成 | 是（默认为“未开始”） |

**每日重置规则（重点）**：
- 每天凌晨00:00（或页面加载检测到跨日时），系统自动将**昨天所有状态为“未完成”（即“未开始”或“进行中”）的计划事项**，复制一份到今天的列表中。
- 复制后的新事项，状态统一重置为“未开始”，日期变为今天。
- 昨天已经“已完成”的事项，不会复制到今日，仅保留在历史记录中供查看。
- **结果**：昨天没做完的事自动顺延到今天，不用手动重新输入；昨天做完的事留在历史里，不影响今日视图。

**列表展示**：
- 默认仅显示“今天”的计划。
- 每个事项前有状态切换按钮（圆形复选框），点击可在“未开始→进行中→已完成”之间循环切换。
- 已完成的事项显示为删除线样式。
- 按优先级（高→中→低）排序。

---

### 模块7：工作待办 ⚠️ 参与每日重置

**功能**：管理工作任务，包含截止日期和优先级。

**字段定义**：
| 字段 | 说明 | 是否必填 |
| :--- | :--- | :--- |
| 截止日期 | 日期选择器 | 是 |
| 事项内容 | 文本输入 | 是 |
| 所属项目 | 文本输入，如“产品V2.0” | 否 |
| 优先级 | 下拉选择：紧急重要 / 重要不紧急 / 紧急不重要 / 不重要不紧急 | 是 |
| 状态 | 下拉选择：待办 / 进行中 / 已完成 / 已延期（系统自动标记） | 是（默认为“待办”） |

**每日重置规则（重点）**：
- 每天凌晨00:00（或页面加载检测到跨日时），系统自动执行以下操作：
  1. **逾期标记**：将所有“截止日期早于今天”且状态仍为“待办”或“进行中”的事项，自动变更为“已延期”状态。
  2. **自动归档**：将所有“已完成”且“完成日期距今超过7天”的事项，自动移入归档（默认列表中不再显示，但仍存在于存储中，用户可通过筛选查看）。
- **结果**：待办列表始终保持清爽，逾期事项自动提醒，长期已完成事项自动收纳。

**列表展示**：
- 默认显示所有“未归档”的事项（即状态为待办/进行中/已完成/已延期，且isArchived为false）。
- 按截止日期从近到远排序。
- 逾期事项（状态为“已延期”）用红色文字或红色标签标出。

---

### 模块8：生活清单

**功能**：管理生活中的各类清单（购物、影单、心愿等）。

**字段定义**：
| 字段 | 说明 | 是否必填 |
| :--- | :--- | :--- |
| 清单类别 | 下拉选择：购物清单 / 影单 / 书单 / 心愿单 / 日常琐事 | 是 |
| 事项内容 | 文本输入 | 是 |
| 目标日期 | 日期选择器 | 否 |
| 完成状态 | 点击复选框切换：未完成 / 已完成 | 是（默认为“未完成”） |

**列表展示**：
- 按类别分组显示（或提供类别筛选）。
- 每条记录前有复选框，点击后切换完成状态，已完成的事项显示删除线。

---

### 模块9：旅游记录

**功能**：记录每次旅行的目的地、时间、花费和心情。

**字段定义**：
| 字段 | 说明 | 是否必填 |
| :--- | :--- | :--- |
| 目的地 | 文本输入 | 是 |
| 出发日期 | 日期选择器 | 是 |
| 结束日期 | 日期选择器 | 是 |
| 同行伙伴 | 文本输入 | 否 |
| 旅行天数 | 根据出发和结束日期自动计算，无需用户填写 | 自动 |
| 总花费 | 数字输入，单位：元 | 否 |
| 游记/心情 | 多行文本 | 否 |

**列表展示**：按出发日期倒序排列，每条记录显示目的地、日期区间、天数。

---

## 五、UI样式规范

### 5.1 色彩系统
| 用途 | 色值 |
| :--- | :--- |
| 页面背景 | #F8F6F3（米白） |
| 卡片背景 | #FFFFFF（纯白） |
| 主色/点缀色 | #D4B8A8（低饱和裸粉） |
| 主文字 | #3D3D3D |
| 辅助文字 | #8A8A8A |
| 边框/分割线 | #ECE8E4 |
| 悬停背景 | #F0E6E0（主色浅化） |

### 5.2 圆角规范
| 元素 | 圆角值 |
| :--- | :--- |
| 卡片容器 | 16px |
| 按钮、标签 | 10px |
| 输入框 | 8px |

### 5.3 阴影规范
| 状态 | 阴影值 |
| :--- | :--- |
| 默认卡片 | 0 4px 16px rgba(0,0,0,0.04) |
| 悬浮卡片 | 0 8px 24px rgba(0,0,0,0.06) |

### 5.4 字体规范
- 字体栈：`-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Inter", sans-serif`
- 标题最大字号：22px，字重550
- 正文：14px，字重400~450
- 辅助文字：12~13px，颜色 #8A8A8A

### 5.5 动效规范
| 场景 | 动画效果 |
| :--- | :--- |
| 面板切换 | 淡入 + 向上移动12px，持续0.35秒 |
| 导航展开/收回 | 宽度变化，持续0.3秒 |
| 复选框点击 | 打勾时带轻微弹性缩放 |
| 卡片悬浮 | 轻微上浮2px + 阴影加深 |
| 列表项悬浮 | 背景色渐变为 #F0E6E0 |

### 5.6 空状态设计
当某个模块没有任何数据时，显示占位提示，例如：
- 阅读模块空状态：“📭 还没有阅读记录，去读一本书吧”
- 计划模块空状态：“📝 今天还没有计划，写一个吧”
- 各模块空状态统一风格：居中显示大号Emoji + 一行提示文字。

---

## 六、交互细节补充

### 6.1 新增记录流程
1. 每个模块顶部都有“新增”表单区域（或在列表上方展开）。
2. 填写完成后，点击“添加”或“确认”按钮，记录立即存入localStorage并刷新列表。
3. 添加成功后，表单自动清空（日期字段重置为今天），并给予轻微成功反馈（如按钮短暂变色或出现对勾）。

### 6.2 删除记录
- 每条记录列表项右侧都配有“删除”按钮（🗑️ 图标）。
- 点击后弹出确认框：“确定要删除这条记录吗？”
- 确认后，该记录从存储中永久移除（不可恢复）。

### 6.3 编辑记录
- 点击列表项内容区域，展开编辑状态（或弹出编辑框）。
- 修改后保存，更新存储中的对应记录。
- 编辑操作应有取消按钮，放弃修改。

### 6.4 数据导入导出
- 页面底部固定区域放置两个按钮：“导出数据”和“导入数据”。
- 导出：将所有数据打包为一个JSON文件，供用户下载保存。
- 导入：用户选择之前导出的JSON文件，系统解析后覆盖当前所有数据（导入前需二次确认“此操作将覆盖现有数据，是否继续？”）。

---

## 七、验收标准

### 7.1 功能验收
- [ ] 所有10个导航面板均可正常切换显示。
- [ ] 顶部时钟实时跳动，格式正确。
- [ ] 每个模块均可正常新增、编辑、删除记录。
- [ ] 跨日检测正常：修改系统日期后刷新页面，能触发重置逻辑并显示提示。
- [ ] 今日计划重置：昨天未完成的事项自动出现在今天列表中，状态为“未开始”。
- [ ] 工作待办重置：逾期事项自动标记为“已延期”，7天前完成的事项自动归档。
- [ ] 数据导出和导入功能正常，导入后数据完整。

### 7.2 视觉验收
- [ ] 整体色调为低饱和裸粉+米白，无高饱和颜色。
- [ ] 所有圆角、阴影、留白符合规范。
- [ ] 导航悬浮展开/收回平滑流畅。
- [ ] 面板切换有淡入上浮动效。
- [ ] 移动端适配良好，导航和内容均不溢出。

### 7.3 数据安全验收
- [ ] 关闭浏览器再打开，所有数据完整保留。
- [ ] 清除浏览器缓存前有导出提示（可选）。
- [ ] 导入数据时有二次确认，防止误操作覆盖。

---

以上为完整需求文档，所有规则、字段、交互均已定稿，可直接交付开发。如有任何疑问，随时沟通。✨
